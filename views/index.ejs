<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
  /* ===== Chessboard Container ===== */
.chessboard {
  display: grid;
  grid-template-columns: repeat(8, 1fr);
  grid-template-rows: repeat(8, 1fr);
  width: 420px;
  height: 420px;

  border-radius: 12px;
  overflow: hidden;

  box-shadow:
    0 12px 30px rgba(0, 0, 0, 0.35),
    inset 0 0 0 2px rgba(255, 255, 255, 0.05);

  background: #111;
  transition: transform 0.3s ease;
}

/* ===== Board Flip ===== */
.flipped {
  transform: rotate(180deg);
}

/* Rotate pieces back so they stay upright */
.flipped .piece {
  transform: rotate(180deg);
}

/* ===== Squares ===== */
.square {
  display: flex;
  align-items: center;
  justify-content: center;
  user-select: none;
}

.square.light {
  background-color: #f0d9b5;
}

.square.dark {
  background-color: #b58863;
}

/* Subtle hover for clarity */
.square:hover {
  box-shadow: inset 0 0 0 2px rgba(0, 0, 0, 0.15);
}

/* ===== Pieces ===== */
.piece {
  font-size: 38px;
  cursor: pointer;
  transition: transform 0.15s ease, opacity 0.15s ease;
}

.piece.white {
  color: #ffffff;
  text-shadow:
    0 1px 2px rgba(0, 0, 0, 0.9),
    0 0 6px rgba(0, 0, 0, 0.4);
}

.piece.black {
  color: #1b1b1b;
  text-shadow:
    0 1px 2px rgba(255, 255, 255, 0.25);
}

/* Draggable feedback */
.piece:active {
  transform: scale(1.08);
}

.dragging {
  opacity: 0.6;
}

/* ===== Demo Mode Toggle Button ===== */
.demo-toggle {
  position: fixed;
  right: 20px;
  top: 50%;
  transform: translateY(-50%);

  padding: 12px 18px;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: 0.3px;

  background: linear-gradient(135deg, #b84b4b, #e35d5d);
  color: #ffffff;

  border: none;
  border-radius: 10px;

  cursor: pointer;
  box-shadow:
    0 8px 20px rgba(0, 0, 0, 0.35),
    inset 0 1px 0 rgba(255, 255, 255, 0.2);

  transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

.demo-toggle:hover {
  background: linear-gradient(135deg, #e35d5d, #ff6b6b);
  box-shadow:
    0 10px 26px rgba(0, 0, 0, 0.45);
}

.demo-toggle:active {
  transform: translateY(-50%) scale(0.95);
}


    </style>
<script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <button id="demoToggle" class="demo-toggle">Demo Mode: OFF</button>

  <div class="w-full h-screen  flex items-center justify-center  bg-zinc-900">
    <div class='chessboard w-96 h-96 bg-red-800'></div>
  </div>
    <script src="https://cdn.socket.io/4.8.1/socket.io.min.js" integrity="sha384-mkQ3/7FUtcGyoppY6bz/PORYoGqOl7/aSUMn2ymDOJcapfS6PHqxhRTMh1RR0Q6+" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

    <script src="/javascripts/chessgame.js"></script>
</body>
</html>